package Seleniumsessions;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class ElementisEnabledSelected {

	//static WebDriver driver;
	public static void main(String[] args) {
		
		//isDisplayed = element is present on the page.
		//isEnabled = element is obviously present on the page but its enabled or not.

//		driver = new ChromeDriver();
//		driver.get("https://classic.freecrm.com/register/");
//		
//		boolean flag =  driver.findElement(By.id("submitButton")).isDisplayed();
//		System.out.println(flag);//true
//		flag = driver.findElement(By.id("submitButton")).isEnabled();
//		System.out.println(flag);//false
		
		
		//isEnabled
		
	//	boolean flag = driver.findElement(By.id("submitButton")).isEnabled();
		//System.out.println(flag);   Before checking the checkbox the flag value is false
		
//		flag = driver.findElement(By.name("agreeTerms")).isSelected();
//		System.out.println(flag);//false
		
		//driver.findElement(By.name("agreeTerms")).click();//select the checkBox
		
		
		
	//    flag = driver.findElement(By.id("submitButton")).isEnabled();
		//System.out.println(flag);//after checking the checkbox the flag value is true and is enable method with give NoSuchElementException In case 
		//element is not found as isDisplayed method.
		
		//isSelected
		
	//	flag = driver.findElement(By.name("agreeTerms")).isSelected();
		//System.out.println(flag);//true
		
	}
		
	}


